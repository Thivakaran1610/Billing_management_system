# Finance & Billing Management System

A Python-based desktop application for managing billing, expenses, and records with Excel automation.

## How to run
pip install -r requirements.txt  
python main.py
